.container
  = simple_form_for(@photo) do |f|
    = f.error_notification
    .row
      .col-s-12
        = f.file_field :image
        = f.input :name
        = f.input :description
        = f.button :submit, class: 'btn-primary'
