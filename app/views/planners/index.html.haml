.container
  .row
    .col-xl-10.offset-xl-1
      .row
        - if @planners.present?
          - @planners.each do |planner|
            .col-sm-12
              = link_to planner_path(planner) do
                %li= planner.name

        - else
          Add some planners!

%br
%br
%br
%br
= link_to 'New Planner', new_planner_path
