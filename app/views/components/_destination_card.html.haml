.col-sm-6.col-lg-4
  .card.mt-3
    = link_to destination, class: 'position-relative' do
      - if destination.photos.present?
        = image_tag destination.photos.first.image.large, class: 'img-fluid card-img-top'
    .card-block
      %h2.card-title.font-weight-bold.mb-1= link_to destination.name, destination
      %h5.font-italic.text-muted.mb-4 #{destination.city + ',' if destination.city.present?} #{destination.state}

      - if destination.excerpt.present?
        %p.font-serif.mb-4= destination.excerpt
      - else
        %p.font-serif.mb-4= destination.about.truncate(100)

      -# - if destination.attractions.present?
      -#   %h6 Top places
      -#   %p
      -#     - destination.attractions.each do |attraction|
      -#       #{attraction.name},

      = link_to 'Read more', destination, class: 'btn btn-secondary'

      -# %p.mb-0
      -#   %i.fa.fa-heart
      -#   12
